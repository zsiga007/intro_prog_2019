## tasks remaining for `ask_player_and_move()`

def get_player_moves( die1, die2 ):
    """
    Return a list of moves the given player wants to make
    """
    ## temporary:
    return []

def is_move_possible( board, player, fromPoint, toPoint ):
    """
    Checks if a given move is legitimate
    (only makes sure that the target point contains
    `player`'s checkers, no checker or exactly one of
    opponent's checkers.
    """
    ## temporary:
    return True

def move( board, player, fromPoint, toPoint ):
    """
    Assuming that the move is legitimate, actually carry it out.
    The `board` gets modified!
    """


